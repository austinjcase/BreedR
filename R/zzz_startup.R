# Function to run when the package is attached
.onAttach <- function(...) {
  
  packageStartupMessage("Loading BreedR...")
  packageStartupMessage("Run the start() function to load a data base.")
  
}
